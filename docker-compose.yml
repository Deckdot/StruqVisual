services:
  db:
    image: postgres:16-alpine
    container_name: struq-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: struq
    ports:
      - "5432:5432"
    volumes:
      - struq-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d struq"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  struq-db-data:
